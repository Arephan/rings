# hello-ring

The contract a real ring would carry. This one is deliberately trivial: write a
few lines to `out/last.md` and stop.

Run it with `rings run examples/hello-ring` to watch the whole cycle — lock,
guard, budget, run, verify, ledger, notify — without spending anything.
