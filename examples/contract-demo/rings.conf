# Copy this fleet into $RINGS_HOME to try it:
#   cp -r examples/contract-demo/* ~/.rings/ && rings contracts
APEX="signed work, and nothing that does not lead to it"
SINKS="revenue:Money in, proof:Machine still verified"
