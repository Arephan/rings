# filer

File the large formal opportunities the scout finds.

Note what is missing here: the file to read. This runbook says what the ring is
for and never says where the work arrives, so the ring cannot do it and no
amount of reading the ledger will tell you — every run exits clean.
