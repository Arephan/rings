# scout

Find work and write one JSON object per line to `out/leads.jsonl`.

Two kinds of row come out of here: `kind: "lead"` for the ordinary ones and
`kind: "tender"` for the large formal ones. Write both. Deciding who acts on
which is not this ring's job — that is what the contract is for.
